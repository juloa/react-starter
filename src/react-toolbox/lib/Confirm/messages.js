import { defineMessages } from "react-intl"

const messages = defineMessages({
  yes : {
    id : "core.yes",
    defaultMessage : "Yes"
  },
  no : {
    id : "core.no",
    defaultMessage : "No"
  }
})

export default messages
